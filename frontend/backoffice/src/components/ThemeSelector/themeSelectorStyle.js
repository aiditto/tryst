const settingsStyle = theme => ({
  singleRow: {
    margin: "2rem 0",
    paddingLeft: "2rem",
    display: "flex",
    alignItems: "center",
    "& p": {
      margin: "0 !important"
    }
  },
  heading: {
    fontSize: "1rem",
    fontWeight: 600
  }
});

export default settingsStyle;
